//Q44: Write a program to find the sum of the series: 1 + 3/4 + 5/6 + 7/8 + … up to n terms.

/*
Sample Test Cases:
Input 1:
3
Output 1:
Approximate sum: 3.3

Input 2:
5
Output 2:
Approximate sum: 4.4

*/
#include <stdio.h>

int main() {
    int n, i;
    double sum = 0.0;

    // Prompt the user for the number of terms
    printf("Input number of terms (n): ");
    scanf("%d", &n);

    // Loop to calculate and sum each term
    for (i = 1; i <= n; i++) {
        // Numerator is 2*i-1, Denominator is 2*i
        // Cast to double to ensure floating-point division
        sum += (double)(2 * i - 1) / (2 * i);
    }

    // Print the final sum, formatted to one decimal place
    printf("Approximate sum: %.1lf\n", sum);

    return 0;
}
