//Q12: Write a program to input an integer and check whether it is positive, negative or zero using nested if–else.

/*
Sample Test Cases:
Input 1:
-5
Output 1:
Negative

Input 2:
0
Output 2:
Zero

Input 3:
10
Output 3:
Positive

*/

int main() {
    int num;

    // Prompt the user to enter an integer
    printf("Input an integer: ");
    
    // Read the integer from the console
    scanf("%d", &num);

    // Check if the number is greater than 0
    if (num > 0) {
        printf("Positive\n");
    } else { // The number must be either negative or zero
        // Check if the number is equal to 0
        if (num == 0) {
            printf("Zero\n");
        } else { // If not positive or zero, it must be negative
            printf("Negative\n");
        }
    }return 0;
}
