Q125: Open an existing file in append mode and allow the user to enter a new line of text. Append the text at the end without overwriting existing content.

/*
Sample Test Cases:
Input 1:
Existing File: data.txt (Content before execution: Hello world)
User Input: This is appended text.
Output 1:
File updated successfully with appended text.

*/
#include <stdio.h>
#include <stdlib.h> // Required for exit() or EXIT_FAILURE
#include <string.h> // Required for strlen()

int main() {
    FILE *file_pointer;
    char textToAppend; // Buffer to store user input

    // Open the file "data.txt" in append mode ("a")
    // This mode ensures that new data is added to the end of the file
    // without erasing the existing content.
    file_pointer = fopen("data.txt", "a");

    if (file_pointer == NULL) {
        perror("Error opening file");
        printf("Please ensure 'data.txt' exists or create it first to run this program.\n");
        return EXIT_FAILURE;
    }

    // Prompt the user for input
    printf("Enter text to append (max 99 chars): ");
    // Clear standard input buffer if necessary before reading a line (platform dependent, often ignored for simple cases)

    // Read the entire line of input from standard input, including spaces
    // fgets reads until a newline or limit is reached, storing the newline character.
    if (fgets(textToAppend, sizeof(textToAppend), stdin) == NULL) {
        printf("Error reading user input.\n");
        fclose(file_pointer);
        return EXIT_FAILURE;
    }

    // Write the user's input string to the file
    fprintf(file_pointer, "%s", textToAppend);

    // Close the file
    fclose(file_pointer);

    // Display confirmation message
    printf("File updated successfully with appended text.\n");

    return 0; // Exit the program successfully
}
