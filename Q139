Q139: Show that enums store integers by printing assigned values.

/*
Sample Test Cases:
Input 1:
No input
Output 1:
SUCCESS=0, FAILURE=1, TIMEOUT=2

*/
#include <stdio.h>

int main() {
    // Define an enumeration with default integer values starting from 0
    enum Status {
        SUCCESS,  // 0
        FAILURE,  // 1
        TIMEOUT   // 2
    };

    // Print the values associated with the enum identifiers using integer format specifiers (%d)
    // This demonstrates that the enum labels are fundamentally treated as integer constants by the compiler.
    printf("SUCCESS=%d, FAILURE=%d, TIMEOUT=%d\n", SUCCESS, FAILURE, TIMEOUT);

    // We can also store an enum value in an integer variable:
    int status_value = FAILURE;
    printf("FAILURE stored in an int variable has value: %d\n", status_value);

    return 0;
}
