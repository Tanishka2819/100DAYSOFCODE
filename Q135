Q135: Assign explicit values starting from 10 and print them.

/*
Sample Test Cases:
Input 1:
No input
Output 1:
A=10
B=11
C=12

*/
#include <stdio.h>

int main() {
    // Define an enumeration and assign an explicit starting value to the first element (A).
    // Subsequent elements (B and C) automatically increment by 1 from the previous value.
    enum ExplicitValues {
        A = 10,
        B, // B automatically becomes 11
        C  // C automatically becomes 12
    };

    // Print the values associated with each enumerator
    printf("A=%d\n", A);
    printf("B=%d\n", B);
    printf("C=%d\n", C);

    return 0;
}
