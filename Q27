//Q27: Write a program to print the sum of the first n odd numbers.

/*
Sample Test Cases:
Input 1:
3
Output 1:
9

Input 2:
5
Output 2:
25

*/

 #include <stdio.h>

int main() {
    int n, i, sum = 0, current_odd = 1;

    // Prompt the user for the number of odd numbers
    printf("Input a number for n: ");
    scanf("%d", &n);

    // Loop n times
    for (i = 0; i < n; i++) {
        sum += current_odd;
        current_odd += 2; // Move to the next odd number
    }

    // Print the sum
    printf("Sum of the first %d odd numbers is: %d\n", n, sum);

    return 0;
}

