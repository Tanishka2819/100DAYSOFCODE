// Q19: Write a program to classify a triangle as Equilateral, Isosceles, or Scalene based on its side lengths.

/*
Sample Test Cases:
Input 1:
3 3 3
Output 1:
Equilateral

Input 2:
3 3 4
Output 2:
Isosceles

Input 3:
2 3 4
Output 3:
Scalene

*/
#include <stdio.h>

int main() {
    int side1, side2, side3;

    // Prompt the user for the three side lengths
    printf("Input three side lengths: ");
    
    // Read the three integers from the console
    scanf("%d %d %d", &side1, &side2, &side3);

    // First check if the sides can form a valid triangle
    if (side1 + side2 <= side3 || side1 + side3 <= side2 || side2 + side3 <= side1) {
        printf("Not a valid triangle\n");
    }
    // Check if it is an Equilateral triangle (all sides are equal)
    else if (side1 == side2 && side2 == side3) {
        printf("Equilateral\n");
    }
    // Check if it is an Isosceles triangle (at least two sides are equal)
    else if (side1 == side2 || side1 == side3 || side2 == side3) {
        printf("Isosceles\n");
    }
    // Otherwise, it must be a Scalene triangle (no sides are equal)
    else {
        printf("Scalene\n");
    }

    return 0;
}
