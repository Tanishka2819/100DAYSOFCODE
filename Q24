//Q24: Write a program to calculate electricity bill based on units consumed with these rates: 
First 100 units at ₹5/unit 
Next 100 units at ₹7/unit 
Next 100 units at ₹10/unit 
Above at ₹12/unit


Sample Test Cases:
Input 1:
50
Output 1:
Bill: ₹250

Input 2:
150
Output 2:
Bill: ₹850

Input 3:
250
Output 3:
Bill: ₹1700

*/
#include <stdio.h>

int main() {
    int units;
    float bill = 0.0;

    // Prompt the user for the number of units consumed
    printf("Input units consumed: ");
    scanf("%d", &units);

    // Calculate bill based on tiered rates
    if (units <= 100) {
        bill = units * 5.0;
    } else if (units <= 200) {
        // First 100 units at ₹5, remaining units at ₹7
        bill = (100 * 5.0) + ((units - 100) * 7.0);
    } else if (units <= 300) {
        // First 100 at ₹5, next 100 at ₹7, remaining at ₹10
        bill = (100 * 5.0) + (100 * 7.0) + ((units - 200) * 10.0);
    } else {
        // First 100 at ₹5, next 100 at ₹7, next 100 at ₹10, remaining at ₹12
        bill = (100 * 5.0) + (100 * 7.0) + (100 * 10.0) + ((units - 300) * 12.0);
    }

    // Print the final bill amount
    printf("Bill: ₹%.0f\n", bill);

    return 0;
}
